Before running, run the install_packages.exe to install required python packages.
Or simply run the following from your command line
pip install mss pygetwindow requests pywin32

Please adjust the config.json before running it
